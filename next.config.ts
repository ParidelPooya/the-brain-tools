import type { NextConfig } from "next";

const nextConfig = {
  output: 'export',
  // If you're not using images, you can skip this
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig

    
